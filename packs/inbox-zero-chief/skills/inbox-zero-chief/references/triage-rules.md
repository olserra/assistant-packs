# Triage rules

> **Status: outline.** Replace each `TODO` and remove this note. Claim this module in issue #2 first.

**When:** as new mail arrives, plus one daily summary at the user's chosen time (default 08:30).
**Goal:** the user sees what needs them in one short message and never wonders if something important is buried.

## Buckets

TODO: define 4-5 buckets and what goes in each. Suggested starting point:

1. **🔴 Needs you** - a real person is waiting on the user.
2. **🟡 To read** - useful, not urgent.
3. **🧾 Receipts & notices** - bills, deliveries, account notices.
4. **📰 Newsletters & promos** - feeds the weekly unsubscribe sweep.
5. TODO: anything else?

## Rule format

TODO: how a rule is written and stored, in plain words the user can edit. Example shape:
`From {{sender}} or about {{topic}} → bucket`

TODO: how the assistant proposes new rules from patterns, and how the user approves them.

## Daily summary

TODO: the message format. Phone-sized, under a minute to read.

## Rules

- Never delete. Moving mail between folders or labels only if the user approved that rule.
- TODO: what to do when a rule conflicts with an urgent sender.
- TODO: what to do on days with no "needs you" mail (send nothing, or one line?).

## Example (shape only, values are illustrative)

```
TODO
```
